# Framework Review

Part B framework notes will live here.

The framework rebuild should mirror the custom runtime CLI surface:

```bash
python -m agent_fw.cli run --task "..."
python -m agent_fw.cli resume <run_id>
python -m agent_fw.cli replay <run_id>
```
